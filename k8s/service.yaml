apiVersion: v1
kind: Service
metadata:
  name: enterprise-agentic-platform-service
spec:
  selector:
    app: enterprise-agentic-platform
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP